# איך מריצים

## התקנה ראשונה
```bash
npm install
```

## הרצה מקומית
```bash
npm run dev
```

פותחים:
```text
http://localhost:3000
```

## בנייה לפרודקשן
```bash
npm run build
npm run start
```

# פריסה ל־Vercel
1. פתח חשבון ב־Vercel
2. העלה את הפרויקט ל־GitHub
3. Import Project ב־Vercel
4. Deploy

# הפיכה לאפליקציה בטלפון

## אייפון
Safari → Share → Add to Home Screen

## אנדרואיד
Chrome → שלוש נקודות → Install App / Add to Home Screen

# Firebase
הקובץ `lib/firebase.ts` כבר מוכן.
צור `.env.local` לפי `.env.example` והכנס את הערכים מפרויקט Firebase.
